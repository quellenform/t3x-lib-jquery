plugin.tx_libjquery {
	settings {
		source = {$plugin.tx_libjquery.settings.source}
		version = {$plugin.tx_libjquery.settings.version}
		localFallback = {$plugin.tx_libjquery.settings.localFallback}
		excludeFromConcatenation = {$plugin.tx_libjquery.settings.excludeFromConcatenation}
		anonymousCrossorigin = {$plugin.tx_libjquery.settings.anonymousCrossorigin}
		localPath = {$plugin.tx_libjquery.settings.localPath}
		minify = {$plugin.tx_libjquery.settings.minify}
	}
}
