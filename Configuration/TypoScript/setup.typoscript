plugin.tx_libjquery {
  enable = {$plugin.tx_libjquery.enable}
  source = {$plugin.tx_libjquery.source}
  version = {$plugin.tx_libjquery.version}
  localFallback = {$plugin.tx_libjquery.localFallback}
  excludeFromConcatenation = {$plugin.tx_libjquery.excludeFromConcatenation}
  anonymousCrossorigin = {$plugin.tx_libjquery.anonymousCrossorigin}
  slim = {$plugin.tx_libjquery.slim}
  minify = {$plugin.tx_libjquery.minify}
  forceOnTop = 1
  addJsFooterLibrary = 0
}
